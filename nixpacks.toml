[phases.setup]
nixPkgs = ["ffmpeg"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["ln -sf $(which ffmpeg) /usr/local/bin/ffmpeg || true", "ln -sf $(which ffprobe) /usr/local/bin/ffprobe || true"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
